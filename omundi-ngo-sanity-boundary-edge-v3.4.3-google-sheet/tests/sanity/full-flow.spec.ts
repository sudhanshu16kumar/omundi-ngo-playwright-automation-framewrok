import {
  test
} from '@playwright/test';

/*
 * Legacy sanity file retained to preserve the original project structure.
 * The maintained real flow is tests/sanity/happy-path.spec.ts.
 */

test.skip(
  'legacy full-flow moved to happy-path.spec.ts',
  async () => {
    // Intentionally skipped.
  }
);
