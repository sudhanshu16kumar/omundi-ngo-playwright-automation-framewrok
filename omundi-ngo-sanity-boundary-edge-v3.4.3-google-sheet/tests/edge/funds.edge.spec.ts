import {
  test
} from '../../fixtures/auth.fixture';

/*
 * Known Fund Transaction Reference security cases were deliberately removed
 * from the normal green Edge run after manual review. Keep this placeholder
 * so the original file/folder structure remains unchanged.
 */

test.describe.skip(
  '@edge Funds text fields',
  () => {
    test(
      'Fund transaction reference security checks temporarily excluded',
      async () => {
        // Intentionally skipped.
      }
    );
  }
);
